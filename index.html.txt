<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Hassany IPA | متجر حساني</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    
    <style>
        /* ==========================================================================
           1. المتغيرات والألوان (Variables)
           ========================================================================== */
        :root {
            --bg-color: #f4f7fe;
            --card-bg: #ffffff;
            --text-main: #111827;
            --text-muted: #6b7280;
            --primary: #6366f1;
            --primary-hover: #4f46e5;
            --secondary: #ec4899;
            --border: #e5e7eb;
            --modal-bg: rgba(0, 0, 0, 0.5);
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.1);
            --glass-bg: rgba(255, 255, 255, 0.85);
        }

        .dark-mode {
            --bg-color: #0b0f19;
            --card-bg: #151c2c;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --primary: #818cf8;
            --secondary: #f472b6;
            --border: #2a3441;
            --modal-bg: rgba(0, 0, 0, 0.8);
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.3);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.4);
            --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.5);
            --glass-bg: rgba(21, 28, 44, 0.85);
        }

        /* ==========================================================================
           2. الإعدادات الأساسية (Reset & Base)
           ========================================================================== */
        * {
            margin: 0; padding: 0; box-sizing: border-box; font-family: 'Cairo', sans-serif;
            -webkit-tap-highlight-color: transparent; outline: none;
        }

        body {
            background-color: var(--bg-color); color: var(--text-main);
            transition: background-color 0.4s ease, color 0.4s ease;
            min-height: 100vh; display: flex; flex-direction: column; overflow-x: hidden;
        }

        .container { flex: 1; max-width: 650px; margin: 0 auto; padding: 20px; width: 100%; }

        /* ==========================================================================
           3. شاشة الانترو (Splash Screen & Intro Animation)
           ========================================================================== */
        #splash-screen {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background-color: var(--bg-color); z-index: 10000;
            display: flex; flex-direction: column; justify-content: center; align-items: center;
            transition: opacity 0.8s ease, visibility 0.8s;
        }

        .wireframe-container { width: 80%; max-width: 300px; display: flex; flex-direction: column; gap: 15px; position: relative; }
        .wf-header { height: 40px; border-radius: 10px; background: transparent; border: 2px solid var(--primary); width: 0; animation: drawBox 0.5s ease forwards; }
        .wf-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
        .wf-box { height: 60px; border-radius: 10px; border: 2px solid var(--border); transform: scaleY(0); transform-origin: top; animation: dropDown 0.4s ease forwards; }
        .wf-box:nth-child(1) { animation-delay: 0.3s; } .wf-box:nth-child(2) { animation-delay: 0.4s; } .wf-box:nth-child(3) { animation-delay: 0.5s; }
        .wf-list { height: 100px; border-radius: 15px; border: 2px solid var(--border); opacity: 0; animation: fadeIn 0.5s 0.8s ease forwards; }

        .splash-text {
            position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%) scale(0.8);
            font-size: 24px; font-weight: 900; white-space: nowrap; opacity: 0;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            animation: textPop 1.5s 1.2s ease forwards; z-index: 2;
            text-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        @keyframes drawBox { to { width: 100%; } }
        @keyframes dropDown { to { transform: scaleY(1); } }
        @keyframes fadeIn { to { opacity: 1; } }
        @keyframes textPop {
            0% { opacity: 0; transform: translate(-50%, -50%) scale(0.8); }
            40% { opacity: 1; transform: translate(-50%, -50%) scale(1.1); }
            100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
        }

        /* ==========================================================================
           4. الهيدر والشريط العلوي
           ========================================================================== */
        header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; padding-top: 10px; }
        .logo { font-size: 26px; font-weight: 900; background: linear-gradient(to right, var(--primary), var(--secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .theme-btn {
            background: var(--card-bg); border: 1px solid var(--border); color: var(--text-main);
            padding: 10px 18px; border-radius: 50px; cursor: pointer; font-weight: 700;
            display: flex; align-items: center; gap: 8px; box-shadow: var(--shadow-sm); transition: 0.3s;
        }
        .theme-btn:active { transform: scale(0.95); }

        /* ==========================================================================
           5. المربعات الإحصائية العلوية
           ========================================================================== */
        .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 25px; }
        .stat-card {
            background: var(--card-bg); border: 1px solid var(--border); border-radius: 20px;
            padding: 18px 5px; text-align: center; cursor: pointer; box-shadow: var(--shadow-sm);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex; flex-direction: column; justify-content: center; align-items: center; gap: 8px;
        }
        .stat-card:hover { transform: translateY(-5px); box-shadow: var(--shadow-md); border-color: var(--primary); }
        .stat-icon svg { width: 28px; height: 28px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
        .stat-title { font-size: 13px; font-weight: 800; color: var(--text-muted); }
        .stat-value { font-size: 24px; font-weight: 900; color: var(--text-main); line-height: 1; }
        
        .card-cert .stat-icon { color: #f59e0b; }
        .card-cert .stat-title { color: #f59e0b; }
        .card-req .stat-icon { color: var(--primary); }
        .card-app .stat-icon { color: #10b981; }

        /* ==========================================================================
           6. البحث والأقسام (مع أيقونات SVG)
           ========================================================================== */
        .search-container { position: relative; margin-bottom: 20px; }
        .search-input {
            width: 100%; padding: 18px 20px 18px 55px; background: var(--card-bg);
            border: 1px solid var(--border); border-radius: 20px; color: var(--text-main);
            font-size: 15px; font-weight: 700; transition: 0.3s; box-shadow: var(--shadow-sm);
        }
        .search-input:focus { border-color: var(--primary); box-shadow: 0 0 0 4px rgba(99,102,241,0.15); }
        .search-icon { position: absolute; left: 20px; top: 50%; transform: translateY(-50%); width: 22px; height: 22px; stroke: var(--text-muted); fill: none; stroke-width: 2; }

        .categories { display: flex; gap: 10px; overflow-x: auto; padding-bottom: 15px; margin-bottom: 15px; scrollbar-width: none; scroll-behavior: smooth; }
        .categories::-webkit-scrollbar { display: none; }
        
        .cat-btn {
            background: var(--card-bg); border: 1px solid var(--border); color: var(--text-main);
            padding: 10px 22px; border-radius: 50px; white-space: nowrap; font-weight: 800;
            font-size: 14px; cursor: pointer; transition: 0.3s; display: flex; align-items: center; gap: 8px;
        }
        .cat-btn svg { width: 18px; height: 18px; stroke: currentColor; fill: none; stroke-width: 2; }
        .cat-btn.active { background: var(--primary); color: white; border-color: var(--primary); transform: scale(1.05); box-shadow: 0 5px 15px rgba(99,102,241,0.3); }
        .fav-cat-btn { border-color: var(--secondary); color: var(--secondary); }
        .fav-cat-btn svg { stroke: var(--secondary); }
        .fav-cat-btn.active { background: var(--secondary); color: white; border-color: var(--secondary); box-shadow: 0 5px 15px rgba(236,72,153,0.3); }

        /* ==========================================================================
           7. شبكة التطبيقات (Cards)
           ========================================================================== */
        .apps-grid { display: flex; flex-direction: column; gap: 18px; }
        .app-card {
            background: var(--card-bg); border: 1px solid var(--border); border-radius: 24px;
            padding: 20px; box-shadow: var(--shadow-sm); transition: 0.3s; position: relative;
            animation: fadeInUp 0.5s ease backwards;
        }
        .app-card:hover { box-shadow: var(--shadow-md); border-color: rgba(99,102,241,0.4); }

        .app-header { display: flex; gap: 16px; align-items: center; margin-bottom: 18px; }
        .app-icon { width: 80px; height: 80px; border-radius: 20px; object-fit: cover; background: var(--border); border: 1px solid var(--border); }
        .app-info { flex: 1; }
        .app-name-row { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 8px; }
        .app-name { font-size: 19px; font-weight: 900; color: var(--text-main); display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
        
        .badge { font-size: 11px; padding: 4px 10px; border-radius: 50px; font-weight: 800; background: rgba(239,68,68,0.1); color: #ef4444; border: 1px solid rgba(239,68,68,0.2); }

        .fav-btn { background: none; border: none; cursor: pointer; padding: 5px; transition: 0.2s; }
        .fav-btn:active { transform: scale(0.7); }
        .fav-btn svg { width: 26px; height: 26px; fill: none; stroke: var(--text-muted); stroke-width: 2; transition: 0.3s; }
        .fav-btn.favorited svg { fill: var(--secondary); stroke: var(--secondary); }

        .app-meta { display: flex; gap: 15px; font-size: 13.5px; color: var(--text-muted); font-weight: 700; align-items: center; }
        .meta-item { display: flex; align-items: center; gap: 6px; }
        .meta-icon { width: 16px; height: 16px; fill: none; stroke: currentColor; stroke-width: 2; }

        .card-actions { display: flex; gap: 12px; margin-top: 10px; }
        .btn-download {
            flex: 1.5; padding: 14px; background: linear-gradient(135deg, var(--primary), var(--primary-hover));
            color: white; text-align: center; border-radius: 18px; text-decoration: none;
            font-weight: 900; font-size: 16px; border: none; cursor: pointer; box-shadow: 0 4px 15px rgba(99,102,241,0.25); transition: 0.3s;
            display: flex; justify-content: center; align-items: center; gap: 8px;
        }
        .btn-download:hover { transform: translateY(-3px); box-shadow: 0 8px 20px rgba(99,102,241,0.4); }
        .btn-download svg { width: 18px; height: 18px; stroke: currentColor; fill: none; stroke-width: 2.5; }

        .btn-details {
            flex: 1; padding: 14px; background: transparent; color: var(--text-main);
            text-align: center; border-radius: 18px; font-weight: 800; font-size: 15px;
            border: 2px solid var(--border); cursor: pointer; display: flex; justify-content: center; align-items: center; gap: 8px; transition: 0.3s;
        }
        .btn-details:hover { background: var(--border); }
        .btn-details svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 2; transition: 0.4s; }
        .btn-details.open svg { transform: rotate(180deg); }

        .app-description-wrapper { max-height: 0; overflow: hidden; transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1); }
        .app-description { margin-top: 15px; padding: 18px; background: rgba(107,114,128,0.05); border-radius: 18px; border: 1px solid var(--border); font-size: 14px; color: var(--text-muted); font-weight: 700; line-height: 1.8; }

        /* ==========================================================================
           8. النوافذ المنبثقة (Modals & Back Button)
           ========================================================================== */
        .modal {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: var(--modal-bg); z-index: 1000; justify-content: center; align-items: center;
            backdrop-filter: blur(8px); opacity: 0; transition: opacity 0.3s ease; padding: 15px;
        }
        .modal.show { opacity: 1; }

        .modal-content {
            background: var(--bg-color); width: 100%; max-width: 500px;
            border-radius: 28px; padding: 0; position: relative; max-height: 90vh; overflow-y: auto;
            transform: scale(0.9) translateY(20px); transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.1);
            box-shadow: var(--shadow-lg); border: 1px solid var(--border);
        }
        .modal.show .modal-content { transform: scale(1) translateY(0); }

        /* هيدر النافذة (زر الرجوع) */
        .modal-header-bar {
            position: sticky; top: 0; background: var(--glass-bg); backdrop-filter: blur(10px);
            padding: 20px; border-bottom: 1px solid var(--border); z-index: 10;
            display: flex; align-items: center; justify-content: space-between; border-radius: 28px 28px 0 0;
        }
        .btn-back {
            background: transparent; border: none; color: var(--primary); font-weight: 800;
            font-size: 16px; cursor: pointer; display: flex; align-items: center; gap: 6px; padding: 5px 10px;
            border-radius: 10px; transition: 0.3s;
        }
        .btn-back:hover { background: rgba(99,102,241,0.1); }
        .btn-back svg { width: 20px; height: 20px; stroke: currentColor; fill: none; stroke-width: 2.5; stroke-linecap: round; stroke-linejoin: round; }
        .modal-main-title { font-size: 18px; font-weight: 900; color: var(--text-main); flex: 1; text-align: center; margin-right: -40px; /* لضبط التوسيط */ }

        .modal-body { padding: 25px 20px; }

        /* ==========================================================================
           9. باقات الشهادات (تأثير النار والترتيب العمودي)
           ========================================================================== */
        .packages-container { display: flex; flex-direction: column; gap: 20px; }

        .package-card {
            background: var(--card-bg); border: 2px solid var(--border); border-radius: 24px;
            padding: 25px; text-align: right; position: relative; overflow: hidden;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex; flex-direction: column; gap: 15px; box-shadow: var(--shadow-sm);
        }

        /* الباقة النارية وتأثير النار (Fire Effect) */
        .pkg-fire { border-color: #ef4444; position: relative; z-index: 1; }
        .pkg-fire::before {
            content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
            background: radial-gradient(circle, rgba(239,68,68,0.15) 0%, transparent 60%);
            opacity: 0; pointer-events: none; transition: opacity 0.4s ease; z-index: -1;
        }
        .pkg-fire:hover {
            transform: translateY(-5px) scale(1.02);
            box-shadow: 0 10px 30px rgba(239,68,68,0.3);
            border-color: #f87171;
        }
        .pkg-fire:hover::before { opacity: 1; animation: rotateFire 4s linear infinite; }

        @keyframes rotateFire { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

        .pkg-super { border-color: #3b82f6; }
        .pkg-super:hover { transform: translateY(-5px); box-shadow: 0 10px 30px rgba(59,130,246,0.3); }
        .pkg-vip { border-color: #f59e0b; }
        .pkg-vip:hover { transform: translateY(-5px); box-shadow: 0 10px 30px rgba(245,158,11,0.3); }

        .pkg-header { display: flex; justify-content: space-between; align-items: center; border-bottom: 2px dashed var(--border); padding-bottom: 15px; }
        .pkg-title { font-weight: 900; font-size: 20px; color: var(--text-main); display: flex; align-items: center; gap: 8px; }
        .pkg-price { font-size: 18px; font-weight: 900; color: #10b981; background: rgba(16,185,129,0.1); padding: 8px 16px; border-radius: 12px; }
        
        .pkg-features { list-style: none; }
        .pkg-features li { font-size: 14.5px; font-weight: 700; color: var(--text-muted); margin-bottom: 12px; display: flex; align-items: center; gap: 10px; }
        .pkg-features li svg { width: 18px; height: 18px; stroke: #10b981; fill: none; stroke-width: 3; flex-shrink: 0; }

        /* فورم الطلب */
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; font-weight: 800; margin-bottom: 8px; font-size: 14px; color: var(--text-main); }
        .form-control {
            width: 100%; padding: 16px; border-radius: 16px; border: 2px solid var(--border);
            background: var(--card-bg); color: var(--text-main); font-weight: 700; font-size: 15px;
            outline: none; transition: 0.3s;
        }
        .form-control:focus { border-color: var(--primary); box-shadow: 0 0 0 4px rgba(99,102,241,0.1); }
        
        .btn-submit {
            width: 100%; padding: 18px; background: linear-gradient(135deg, var(--primary), var(--primary-hover));
            color: white; border: none; border-radius: 16px; font-weight: 900; font-size: 17px; cursor: pointer;
            transition: all 0.3s; display: flex; justify-content: center; align-items: center; gap: 10px;
            box-shadow: 0 5px 15px rgba(99,102,241,0.3); text-decoration: none;
        }
        .btn-submit:hover { transform: translateY(-3px); box-shadow: 0 10px 25px rgba(99,102,241,0.5); }
        .btn-submit svg { width: 22px; height: 22px; fill: white; }

        /* ==========================================================================
           10. قسم الدعم الفني (Footer)
           ========================================================================== */
        .site-footer {
            margin-top: 50px; padding: 30px 20px; background: var(--card-bg); border-top: 1px solid var(--border);
            text-align: center; border-radius: 30px 30px 0 0; box-shadow: 0 -5px 20px rgba(0,0,0,0.02);
        }
        .footer-content { display: flex; flex-direction: column; align-items: center; gap: 15px; }
        .footer-title { font-size: 18px; font-weight: 900; color: var(--text-main); display: flex; align-items: center; gap: 8px; }
        .footer-title svg { width: 24px; height: 24px; stroke: var(--primary); fill: none; stroke-width: 2; }
        .footer-link {
            display: inline-flex; align-items: center; gap: 10px; background: rgba(59,130,246,0.1); color: #3b82f6;
            padding: 12px 24px; border-radius: 50px; text-decoration: none; font-weight: 800; font-size: 16px; transition: 0.3s;
        }
        .footer-link:hover { background: #3b82f6; color: white; transform: scale(1.05); }
        .footer-link svg { width: 20px; height: 20px; fill: currentColor; }
        .footer-copy { font-size: 13px; font-weight: 600; color: var(--text-muted); margin-top: 15px; }

        /* ==========================================================================
           11. الإشعارات والأنيميشن
           ========================================================================== */
        @keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes shimmer { 0% { background-position: -1000px 0; } 100% { background-position: 1000px 0; } }
        
        .skeleton-card {
            height: 180px; background: linear-gradient(to right, var(--card-bg) 8%, var(--border) 18%, var(--card-bg) 33%);
            background-size: 1000px 100%; animation: shimmer 2s infinite linear; border-radius: 24px; border: 1px solid var(--border);
        }

        .toast {
            position: fixed; bottom: 30px; left: 50%; transform: translateX(-50%) translateY(100px);
            background: var(--text-main); color: var(--bg-color); padding: 14px 28px;
            border-radius: 50px; font-weight: 800; font-size: 15px; opacity: 0;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); z-index: 9999; box-shadow: var(--shadow-lg);
            display: flex; align-items: center; gap: 10px;
        }
        .toast.show { transform: translateX(-50%) translateY(0); opacity: 1; }
        .toast svg { width: 20px; height: 20px; stroke: currentColor; fill: none; stroke-width: 2; }
    </style>
</head>
<body>

    <div id="splash-screen">
        <div class="wireframe-container">
            <div class="wf-header"></div>
            <div class="wf-grid">
                <div class="wf-box"></div><div class="wf-box"></div><div class="wf-box"></div>
            </div>
            <div class="wf-list"></div>
            <div class="wf-list"></div>
        </div>
        <div class="splash-text">مرحباً في Hassany IPA</div>
    </div>

    <div class="container" id="main-content" style="opacity:0; transition: opacity 0.5s;">
        <header>
            <div class="logo">Hassany IPA</div>
            <button class="theme-btn" onclick="toggleTheme()">
                <svg id="theme-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path></svg>
                <span id="theme-text">ليلي</span>
            </button>
        </header>

        <div class="stats-grid">
            <div class="stat-card card-cert" onclick="openModal('certModal')">
                <div class="stat-icon"><svg viewBox="0 0 24 24"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon></svg></div>
                <div class="stat-title">شراء شهادة</div>
            </div>
            <div class="stat-card card-req" onclick="openModal('requestModal')">
                <div class="stat-icon"><svg viewBox="0 0 24 24"><rect x="2" y="6" width="20" height="12" rx="2" ry="2"></rect><circle cx="16" cy="12" r="1"></circle><circle cx="12" cy="12" r="1"></circle><path d="M6 12h4"></path><path d="M8 10v4"></path></svg></div>
                <div class="stat-title">اطلب تطبيق</div>
            </div>
            <div class="stat-card card-app">
                <div class="stat-icon" style="display:flex; justify-content:center;"><span class="stat-value" id="app-counter">0</span></div>
                <div class="stat-title">تطبيق متاح</div>
            </div>
        </div>

        <div class="search-container">
            <svg class="search-icon" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
            <input type="text" class="search-input" id="searchInput" placeholder="ابحث عن تطبيق أو لعبة..." onkeyup="filterApps()">
        </div>

        <div class="categories" id="categories-container">
            <button class="cat-btn fav-cat-btn" onclick="filterFavorites(this)">
                <svg viewBox="0 0 24 24"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
                المفضلة
            </button>
            <button class="cat-btn active" onclick="filterCategory('الكل', this)">
                <svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect x="14" y="14" width="7" height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg>
                الكل
            </button>
            </div>

        <div class="apps-grid" id="apps-container">
            <div class="skeleton-card"></div><div class="skeleton-card"></div><div class="skeleton-card"></div>
        </div>
    </div>

    <footer class="site-footer">
        <div class="footer-content">
            <div class="footer-title">
                <svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
                مركز الدعم الفني
            </div>
            <a href="https://t.me/OM_G9" target="_blank" class="footer-link">
                <svg viewBox="0 0 24 24"><path d="M21.198 2.433a2.242 2.242 0 0 0-1.022.215l-18.6 7.143c-1.25.48-1.246 1.136-.214 1.45l4.76 1.488 1.455 4.542c.22.686.113.818.824.818.536 0 .753-.25 1.144-.633l2.748-2.67 5.71 4.215c1.05.58 1.8.28 2.062-.973l3.733-17.587c.394-1.57-.592-2.28-1.6-1.908z"></path></svg>
                تواصل معنا @OM_G9
            </a>
            <div class="footer-copy">© 2026 Hassany Store. جميع الحقوق محفوظة.</div>
        </div>
    </footer>

    <div class="modal" id="requestModal">
        <div class="modal-content">
            <div class="modal-header-bar">
                <button class="btn-back" onclick="closeModal('requestModal')">
                    <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg> رجوع
                </button>
                <div class="modal-main-title">اطلب لعبتك</div>
                <div style="width:70px;"></div> </div>
            <div class="modal-body">
                <div class="form-group">
                    <label>اسم التطبيق/اللعبة</label>
                    <input type="text" id="reqName" class="form-control" placeholder="اكتب اسم اللعبة هنا...">
                </div>
                <div class="form-group">
                    <label>رابط اللعبة من الآب ستور</label>
                    <input type="url" id="reqLink" class="form-control" placeholder="https://apps.apple.com/...">
                </div>
                <div class="form-group">
                    <label>مميزات الهاك المطلوبة</label>
                    <textarea id="reqFeatures" class="form-control" rows="4" placeholder="مثال: رادار، إخفاء عشب، إلخ..."></textarea>
                </div>
                <button class="btn-submit" onclick="sendToTelegram()">
                    إرسال الطلب <svg viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
                </button>
            </div>
        </div>
    </div>

    <div class="modal" id="certModal">
        <div class="modal-content">
            <div class="modal-header-bar">
                <button class="btn-back" onclick="closeModal('certModal')">
                    <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg> رجوع
                </button>
                <div class="modal-main-title">الباقات المتاحة</div>
                <div style="width:70px;"></div>
            </div>
            <div class="modal-body">
                <div class="packages-container">
                    
                    <div class="package-card pkg-fire">
                        <div class="pkg-header">
                            <div class="pkg-title"><span style="font-size:24px;">🔥</span> الباقة النارية</div>
                            <div class="pkg-price">10 آلاف </div>

                        </div>
                        <ul class="pkg-features">
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> شهادة أبل ذهبية رسمية </li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> مكتبة ضخمة معدلة ومحذوفة</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> دعم كافة برامج التوقيع</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> استبدال (شهر) | المدة سنة ✅</li>
                        </ul>
                    </div>

                    <div class="package-card pkg-vip">
                        <div class="pkg-header">
                            <div class="pkg-title"><span style="font-size:24px;">👑</span> باقة الـ VIP</div>
                            <div class="pkg-price">15 آلاف </div>
                            
                        </div>
                        <ul class="pkg-features">
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> شهادة أبل ذهبية رسمية </li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> مكتبة ضخمة معدلة ومحذوفة</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> دعم كافة برامج التوقيع</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> استبدال (3 أشهر) | المدة سنة ✅</li>
                        </ul>
                    </div>

                    <div class="package-card pkg-super">
                        <div class="pkg-header">
                            <div class="pkg-title"><span style="font-size:24px;">⚡</span> الباقة السوبر</div>
                            <div class="pkg-price">25 آلاف </div>
                            
                        </div>
                        <ul class="pkg-features">
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> شهادة أبل ذهبية رسمية </li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> مكتبة ضخمة معدلة ومحذوفة</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> دعم كافة برامج التوقيع</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg>م-اطول مده ضمان ممكنه</li>
                            <li><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg> استبدال (6 أشهر) | المدة سنة ✅</li>
                        </ul>
                    </div>

                </div>
                
                <a href="https://t.me/OM_G9" target="_blank" class="btn-submit" style="margin-top:25px;">
                    اشترك الآن عبر التليكرام <svg viewBox="0 0 24 24"><path d="M21.198 2.433a2.242 2.242 0 0 0-1.022.215l-18.6 7.143c-1.25.48-1.246 1.136-.214 1.45l4.76 1.488 1.455 4.542c.22.686.113.818.824.818.536 0 .753-.25 1.144-.633l2.748-2.67 5.71 4.215c1.05.58 1.8.28 2.062-.973l3.733-17.587c.394-1.57-.592-2.28-1.6-1.908z"></path></svg>
                </a>
            </div>
        </div>
    </div>

    <div class="toast" id="toastMsg">
        <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"></polyline></svg>
        <span id="toastText">تمت العملية بنجاح</span>
    </div>

    <script>
        // إعدادات الانترو (Splash Screen)
        window.addEventListener('load', () => {
            setTimeout(() => {
                const splash = document.getElementById('splash-screen');
                const main = document.getElementById('main-content');
                splash.style.opacity = '0';
                splash.style.visibility = 'hidden';
                main.style.opacity = '1';
            }, 2600); // مدة عرض الانترو (2.6 ثانية)
        });

        // متغيرات البيانات
        const sheetID = '12OIHyFai2ylBaJYJpoz91uPfeu37dgqJk8Y5w8U9-ao';
        const url = `https://docs.google.com/spreadsheets/d/${sheetID}/gviz/tq?tqx=out:json`;
        let allApps = [];
        let favorites = JSON.parse(localStorage.getItem('hassany_favorites')) || [];

        // نظام المظهر الليلي/النهاري
        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            const isDark = document.body.classList.contains('dark-mode');
            const text = document.getElementById('theme-text');
            const icon = document.getElementById('theme-icon');
            
            if(isDark) {
                text.innerText = 'نهاري';
                icon.innerHTML = '<circle cx="12" cy="12" r="5"></circle><line x1="12" y1="1" x2="12" y2="3"></line><line x1="12" y1="21" x2="12" y2="23"></line><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line><line x1="1" y1="12" x2="3" y2="12"></line><line x1="21" y1="12" x2="23" y2="12"></line><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>';
            } else {
                text.innerText = 'ليلي';
                icon.innerHTML = '<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>';
            }
        }

        // تحكم النوافذ مع أزرار الرجوع
        function openModal(id) {
            const modal = document.getElementById(id);
            modal.style.display = 'flex';
            setTimeout(() => modal.classList.add('show'), 10);
            document.body.style.overflow = 'hidden'; // منع النزول بالخلفية
        }
        function closeModal(id) {
            const modal = document.getElementById(id);
            modal.classList.remove('show');
            setTimeout(() => {
                modal.style.display = 'none';
                document.body.style.overflow = 'auto'; // إرجاع النزول
            }, 400);
        }

        // إغلاق النافذة عند الضغط بالخارج
        window.onclick = function(event) {
            if (event.target.classList.contains('modal')) {
                closeModal(event.target.id);
            }
        }

        // نظام الإشعارات (Toasts)
        function showToast(msg) {
            const toast = document.getElementById('toastMsg');
            document.getElementById('toastText').innerText = msg;
            toast.classList.add('show');
            setTimeout(() => toast.classList.remove('show'), 3000);
        }

        // المفضلة
        function toggleFavorite(appName, btnElement) {
            const index = favorites.indexOf(appName);
            if(index > -1) {
                favorites.splice(index, 1);
                btnElement.classList.remove('favorited');
                showToast('تم الإزالة من المفضلة');
            } else {
                favorites.push(appName);
                btnElement.classList.add('favorited');
                showToast('تمت الإضافة للمفضلة بنجاح 💖');
            }
            localStorage.setItem('hassany_favorites', JSON.stringify(favorites));
        }

        // زر التفاصيل الأكورديون
        function toggleDetails(btn) {
            btn.classList.toggle('open');
            const wrapper = btn.closest('.app-card').querySelector('.app-description-wrapper');
            if (wrapper.style.maxHeight) {
                wrapper.style.maxHeight = null;
                btn.innerHTML = 'التفاصيل <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>';
            } else {
                wrapper.style.maxHeight = wrapper.scrollHeight + "px";
                btn.innerHTML = 'إخفاء <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>';
            }
        }

        // إرسال طلب التليكرام
        function sendToTelegram() {
            const n = document.getElementById('reqName').value;
            const l = document.getElementById('reqLink').value;
            const f = document.getElementById('reqFeatures').value;
            if(!n) { alert('الرجاء إدخال اسم اللعبة أو التطبيق!'); return; }
            const msg = `*طلب لعبة جديدة* 🎮\n\n📌 الاسم: ${n}\n🔗 الرابط: ${l}\n⚙️ المميزات: ${f}`;
            window.open(`https://t.me/OM_G9?text=${encodeURIComponent(msg)}`, '_blank');
            closeModal('requestModal');
            document.getElementById('reqName').value = '';
            document.getElementById('reqLink').value = '';
            document.getElementById('reqFeatures').value = '';
        }

        // جلب الداتا من جدول غوغل
        fetch(url)
            .then(res => res.text())
            .then(text => {
                const data = JSON.parse(text.substring(47, text.length - 2));
                const rows = data.table.rows;
                allApps = [];
                let cats = new Set();
                
                let start = (rows[0].c[0] && rows[0].c[0].v === 'اسم التطبيق') ? 1 : 0;

                for (let i = start; i < rows.length; i++) {
                    const r = rows[i].c;
                    if (!r || !r[0] || !r[0].v) continue;
                    const app = {
                        name: r[0]?r[0].v:'-', icon: r[1]?r[1].v:'',
                        category: r[2]?r[2].v:'عام', version: r[3]?r[3].v:'v1.0',
                        size: r[4]?r[4].v:'-', badge: r[5]?r[5].v:'',
                        desc: r[6]?r[6].v:'لا يوجد وصف متاح.', link: r[7]?r[7].v:'#'
                    };
                    allApps.push(app);
                    if(app.category) cats.add(app.category);
                }

                document.getElementById('app-counter').innerText = allApps.length;

                // رسم الأقسام بأيقونة مجلد SVG
                const catCont = document.getElementById('categories-container');
                cats.forEach(c => {
                    const folderSvg = `<svg viewBox="0 0 24 24"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path></svg>`;
                    catCont.innerHTML += `<button class="cat-btn" onclick="filterCategory('${c}', this)">${folderSvg} ${c}</button>`;
                });

                renderApps(allApps);
            })
            .catch(err => {
                document.getElementById('apps-container').innerHTML = '<div style="text-align:center; padding:40px; color:#ef4444; font-weight:900;">❌ حدث خطأ، يرجى التأكد من صلاحيات جدول غوغل.</div>';
            });

        // رسم كروت التطبيقات
        function renderApps(apps) {
            const cont = document.getElementById('apps-container');
            cont.innerHTML = '';
            
            if(apps.length === 0) {
                cont.innerHTML = '<div style="text-align:center; padding:50px; font-weight:800; color:var(--text-muted);">لم يتم العثور على نتائج 🔍</div>';
                return;
            }

            apps.forEach((app, i) => {
                const isFav = favorites.includes(app.name) ? 'favorited' : '';
                const delay = i * 0.05;

                let badgeHTML = '';
                if(app.badge && app.badge !== 'null' && app.badge.trim() !== '') {
                    badgeHTML = `<span class="badge">${app.badge}</span>`;
                }

                const verIcon = `<svg class="meta-icon" viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path><polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline><line x1="12" y1="22.08" x2="12" y2="12"></line></svg>`;
                const sizeIcon = `<svg class="meta-icon" viewBox="0 0 24 24"><path d="M22 12h-4l-3 9L9 3l-3 9H2"></path></svg>`;

                const card = `
                    <div class="app-card" style="animation-delay: ${delay}s">
                        <div class="app-header">
                            <img class="app-icon" src="${app.icon}" alt="icon" onerror="this.src='https://via.placeholder.com/150'">
                            <div class="app-info">
                                <div class="app-name-row">
                                    <div class="app-name">${app.name} ${badgeHTML}</div>
                                    <button class="fav-btn ${isFav}" onclick="toggleFavorite('${app.name}', this)">
                                        <svg viewBox="0 0 24 24"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
                                    </button>
                                </div>
                                <div class="app-meta">
                                    <span class="meta-item">${verIcon} ${app.version}</span>
                                    <span>•</span>
                                    <span class="meta-item">${sizeIcon} ${app.size}</span>
                                </div>
                            </div>
                        </div>
                        <div class="card-actions">
                            <a href="${app.link}" target="_blank" class="btn-download">
                                <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg> تحميل
                            </a>
                            <button class="btn-details" onclick="toggleDetails(this)">
                                التفاصيل <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>
                            </button>
                        </div>
                        <div class="app-description-wrapper">
                            <div class="app-description">${app.desc}</div>
                        </div>
                    </div>
                `;
                cont.innerHTML += card;
            });
        }

        // الفلاتر
        function filterApps() {
            const q = document.getElementById('searchInput').value.toLowerCase();
            renderApps(allApps.filter(a => a.name.toLowerCase().includes(q)));
            updateActiveBtn(null);
        }
        function filterCategory(cat, btn) {
            document.getElementById('searchInput').value = '';
            renderApps(cat === 'الكل' ? allApps : allApps.filter(a => a.category === cat));
            updateActiveBtn(btn);
        }
        function filterFavorites(btn) {
            document.getElementById('searchInput').value = '';
            renderApps(allApps.filter(a => favorites.includes(a.name)));
            updateActiveBtn(btn);
        }
        function updateActiveBtn(activeBtn) {
            document.querySelectorAll('.cat-btn').forEach(b => b.classList.remove('active'));
            if(activeBtn) activeBtn.classList.add('active');
        }
    </script>
</body>
</html>
